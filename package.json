{
  "name": "medicare-website",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Accessible, responsive pharmacy catalogue portfolio demo",
  "scripts": {
    "test": "node --test",
    "check": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

